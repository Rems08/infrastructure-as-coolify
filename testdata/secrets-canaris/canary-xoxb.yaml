api_version: iac-coolify/v1
kind: Application
metadata:
  name: canary-xoxb
  project: test
  environment: staging
spec:
  build_pack: dockerfile
  destination:
    server: localhost
    network: coolify
  port: 3000
  env_vars:
    - name: SECRET_TOKEN
      value: xoxb-canary-not-a-real-token-000000
