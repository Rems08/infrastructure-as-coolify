api_version: iac-coolify/v1
kind: Application
metadata:
  name: custom-secret-5
  project: test
  environment: staging
spec:
  build_pack: dockerfile
  destination:
    server: localhost
    network: coolify
  port: 3000
  env_vars:
    - name: API_KEY
      value: b64ENCODED+slash/and=padding9876543210abcd
