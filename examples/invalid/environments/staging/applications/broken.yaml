# Intentionally invalid: bad build_pack value and a missing required port.
# Used by the validate test-suite and the `validate examples/invalid/` smoke.
api_version: iac-coolify/v1
kind: Application
metadata:
  name: broken
  project: demo
  environment: staging
spec:
  build_pack: rust
  destination:
    server: localhost
    network: coolify
  env_vars:
    - name: NODE_ENV
      value: production
