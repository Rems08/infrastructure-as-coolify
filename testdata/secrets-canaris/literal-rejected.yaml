api_version: iac-coolify/v1
kind: Application
metadata:
  name: literal-rejected
  project: test
  environment: staging
spec:
  build_pack: dockerfile
  destination:
    server: localhost
    network: coolify
  port: 3000
  env_vars:
    - name: DATABASE_URL
      value_secret: postgres://user:pass@host/db
